<!-- 関係性管理一覧 -->
<div class="space-y-6">
  <!-- ページヘッダー -->
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">
        <i class="fas fa-users mr-3 text-green-600"></i>
        関係性管理
      </h1>
      <p class="text-gray-600 mt-1">ギフト相手との関係性の管理</p>
    </div>
    
    <!-- 新規作成ボタン -->
    <div class="flex space-x-3">
      <%= link_to new_admin_relationship_path,
          class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" do %>
        <i class="fas fa-plus mr-2"></i>
        新しい関係性を追加
      <% end %>
    </div>
  </div>

  <!-- 統計サマリー -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-green-500">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <i class="fas fa-users text-2xl text-green-600"></i>
        </div>
        <div class="ml-5 w-0 flex-1">
          <dl>
            <dt class="text-sm font-medium text-gray-500 truncate">
              総関係性数
            </dt>
            <dd class="text-lg font-medium text-gray-900">
              <%= @relationships.respond_to?(:total_count) ? @relationships.total_count : Relationship.count %>件
            </dd>
          </dl>
        </div>
      </div>
    </div>

    <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-blue-500">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <i class="fas fa-user-friends text-2xl text-blue-600"></i>
        </div>
        <div class="ml-5 w-0 flex-1">
          <dl>
            <dt class="text-sm font-medium text-gray-500 truncate">
              使用中の関係性
            </dt>
            <dd class="text-lg font-medium text-gray-900">
              <%= Relationship.joins(:gift_people).distinct.count %>件
            </dd>
          </dl>
        </div>
      </div>
    </div>

    <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-purple-500">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <i class="fas fa-star text-2xl text-purple-600"></i>
        </div>
        <div class="ml-5 w-0 flex-1">
          <dl>
            <dt class="text-sm font-medium text-gray-500 truncate">
              人気の関係性
            </dt>
            <dd class="text-lg font-medium text-gray-900">
              <% popular_relationship = Relationship.joins(:gift_people).group(:id).order('COUNT(gift_people.id) DESC').first %>
              <%= popular_relationship&.name || "未使用" %>
            </dd>
          </dl>
        </div>
      </div>
    </div>
  </div>

  <!-- 関係性一覧テーブル -->
  <div class="bg-white rounded-lg shadow-md overflow-hidden">
    <div class="px-4 py-5 sm:p-6">
      <div class="flex items-center justify-between mb-4">
        <h3 class="text-lg font-medium text-gray-900">
          関係性一覧
        </h3>
        <div class="text-sm text-gray-500">
          合計 <%= @relationships.respond_to?(:total_count) ? @relationships.total_count : Relationship.count %> 件
        </div>
      </div>
    </div>

    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              関係性名
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              使用ギフト相手数
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              作成日
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              アクション
            </th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @relationships.each do |relationship| %>
            <tr class="hover:bg-gray-50">
              <!-- 関係性名 -->
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="flex items-center">
                  <div class="flex-shrink-0">
                    <div class="w-10 h-10 rounded-lg bg-green-100 flex items-center justify-center">
                      <i class="fas fa-user-tag text-green-600"></i>
                    </div>
                  </div>
                  <div class="ml-4">
                    <div class="text-sm font-medium text-gray-900">
                      <%= relationship.display_name %>
                    </div>
                    <% if relationship.name.length > 30 %>
                      <div class="text-sm text-gray-500">
                        ID: <%= relationship.id %>
                      </div>
                    <% end %>
                  </div>
                </div>
              </td>

              <!-- 使用ギフト相手数 -->
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="flex items-center">
                  <span class="text-sm font-medium text-gray-900">
                    <%= relationship.gift_people.count %>
                  </span>
                  <span class="text-sm text-gray-500 ml-1">人</span>
                  <% if relationship.gift_people.count > 0 %>
                    <span class="ml-2 inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                      使用中
                    </span>
                  <% else %>
                    <span class="ml-2 inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                      未使用
                    </span>
                  <% end %>
                </div>
              </td>

              <!-- 作成日 -->
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                <%= l(relationship.created_at, format: :short) %>
              </td>

              <!-- アクション -->
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium space-x-2">
                <!-- 順序変更ボタン -->
                <% unless relationship.position == 1 %>
                  <%= button_to move_up_admin_relationship_path(relationship),
                      method: :patch,
                      class: "text-green-600 hover:text-green-900 border-0 bg-transparent p-0 cursor-pointer",
                      form: { style: "display: inline;" },
                      title: "上に移動" do %>
                    <i class="fas fa-chevron-up"></i>
                  <% end %>
                <% else %>
                  <span class="text-gray-300" title="最上位のため上に移動できません">
                    <i class="fas fa-chevron-up"></i>
                  </span>
                <% end %>

                <% unless relationship.position == Relationship.maximum(:position) %>
                  <%= button_to move_down_admin_relationship_path(relationship),
                      method: :patch,
                      class: "text-green-600 hover:text-green-900 border-0 bg-transparent p-0 cursor-pointer",
                      form: { style: "display: inline;" },
                      title: "下に移動" do %>
                    <i class="fas fa-chevron-down"></i>
                  <% end %>
                <% else %>
                  <span class="text-gray-300" title="最下位のため下に移動できません">
                    <i class="fas fa-chevron-down"></i>
                  </span>
                <% end %>

                <%= link_to "編集", edit_admin_relationship_path(relationship), 
                    class: "text-blue-600 hover:text-blue-900" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <!-- ページネーション -->
    <% if @relationships.respond_to?(:current_page) && @relationships.total_pages > 1 %>
      <div class="px-6 py-4 border-t border-gray-200">
        <nav class="flex items-center justify-between">
          <!-- 前のページボタン -->
          <div class="flex-1 flex justify-start">
            <% if @relationships.prev_page %>
              <%= link_to admin_relationships_path(page: @relationships.prev_page),
                  class: "relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 transition-colors" do %>
                <i class="fas fa-chevron-left mr-2"></i>
                前へ
              <% end %>
            <% else %>
              <span class="relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-400 bg-gray-100 cursor-not-allowed">
                <i class="fas fa-chevron-left mr-2"></i>
                前へ
              </span>
            <% end %>
          </div>

          <!-- ページ番号 -->
          <div class="hidden md:flex">
            <% start_page = [@relationships.current_page - 2, 1].max %>
            <% end_page = [start_page + 4, @relationships.total_pages].min %>
            <% start_page = [end_page - 4, 1].max if end_page - start_page < 4 %>

            <!-- ページ番号範囲 -->
            <% (start_page..end_page).each do |page_num| %>
              <% if page_num == @relationships.current_page %>
                <span aria-current="page" class="relative inline-flex items-center px-3 py-2 border border-green-500 bg-green-50 text-sm font-medium text-green-600">
                  <%= page_num %>
                </span>
              <% else %>
                <%= link_to admin_relationships_path(page: page_num),
                    class: "relative inline-flex items-center px-3 py-2 border border-gray-300 bg-white text-sm font-medium text-gray-500 hover:bg-gray-50" do %>
                  <%= page_num %>
                <% end %>
              <% end %>
            <% end %>
          </div>

          <!-- 次のページボタン -->
          <div class="flex-1 flex justify-end">
            <% if @relationships.next_page %>
              <%= link_to admin_relationships_path(page: @relationships.next_page),
                  class: "ml-3 relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 transition-colors" do %>
                次へ
                <i class="fas fa-chevron-right ml-2"></i>
              <% end %>
            <% else %>
              <span class="ml-3 relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-400 bg-gray-100 cursor-not-allowed">
                次へ
                <i class="fas fa-chevron-right ml-2"></i>
              </span>
            <% end %>
          </div>
        </nav>

        <!-- ページ情報 -->
        <div class="mt-3 text-center text-sm text-gray-700">
          <%= @relationships.current_page %>/<%= @relationships.total_pages %>ページ 
          （全<%= @relationships.total_count %>件中 
          <%= (@relationships.current_page - 1) * @relationships.limit_value + 1 %>-<%= [@relationships.current_page * @relationships.limit_value, @relationships.total_count].min %>件を表示）
        </div>
      </div>
    <% end %>
  </div>

  <!-- 空の状態表示 -->
  <% if @relationships.empty? %>
    <div class="bg-white rounded-lg shadow-md p-12 text-center">
      <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
        <i class="fas fa-users text-2xl text-gray-400"></i>
      </div>
      <h3 class="text-lg font-medium text-gray-900 mb-2">関係性がありません</h3>
      <p class="text-gray-500 mb-4">
        最初の関係性を作成してギフト相手との関係を定義しましょう。
      </p>
      <%= link_to new_admin_relationship_path,
          class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" do %>
        <i class="fas fa-plus mr-2"></i>
        最初の関係性を作成
      <% end %>
    </div>
  <% end %>

  <!-- ヒント -->
  <div class="bg-green-50 border border-green-200 rounded-lg p-4">
    <div class="flex">
      <div class="flex-shrink-0">
        <i class="fas fa-info-circle text-green-400"></i>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-green-800">
          関係性管理について
        </h3>
        <div class="mt-2 text-sm text-green-700">
          <ul class="list-disc pl-5 space-y-1">
            <li>関係性はギフト相手との関係を表します</li>
            <li>既にギフト相手で使用されている関係性は削除できません</li>
            <li>よく使われる関係性例：家族、友人、恋人、同僚、同級生、先輩、後輩など</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>