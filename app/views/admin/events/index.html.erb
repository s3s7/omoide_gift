<!-- イベント管理一覧 -->
<div class="space-y-6">
  <!-- ページヘッダー -->
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">
        <i class="fas fa-calendar-alt mr-3 text-orange-600"></i>
        イベント管理
      </h1>
      <p class="text-gray-600 mt-1">ギフト記録で使用されるイベントの管理</p>
    </div>
    
    <!-- 新規作成ボタン -->
    <div class="flex space-x-3">
      <%= link_to new_admin_event_path,
          class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-orange-600 hover:bg-orange-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500" do %>
        <i class="fas fa-plus mr-2"></i>
        新しいイベントを追加
      <% end %>
    </div>
  </div>

  <!-- 統計サマリー -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-orange-500">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <i class="fas fa-calendar-alt text-2xl text-orange-600"></i>
        </div>
        <div class="ml-5 w-0 flex-1">
          <dl>
            <dt class="text-sm font-medium text-gray-500 truncate">
              総イベント数
            </dt>
            <dd class="text-lg font-medium text-gray-900">
              <%= @events.total_count if @events.respond_to?(:total_count) || Event.count %>件
            </dd>
          </dl>
        </div>
      </div>
    </div>

    <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-blue-500">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <i class="fas fa-gift text-2xl text-blue-600"></i>
        </div>
        <div class="ml-5 w-0 flex-1">
          <dl>
            <dt class="text-sm font-medium text-gray-500 truncate">
              使用中のイベント
            </dt>
            <dd class="text-lg font-medium text-gray-900">
              <%= Event.joins(:gift_records).distinct.count %>件
            </dd>
          </dl>
        </div>
      </div>
    </div>

    <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-green-500">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <i class="fas fa-star text-2xl text-green-600"></i>
        </div>
        <div class="ml-5 w-0 flex-1">
          <dl>
            <dt class="text-sm font-medium text-gray-500 truncate">
              人気のイベント
            </dt>
            <dd class="text-lg font-medium text-gray-900">
              <% popular_event = Event.popular_events(1).first %>
              <%= popular_event&.name || "未使用" %>
            </dd>
          </dl>
        </div>
      </div>
    </div>
  </div>

  <!-- イベント一覧テーブル -->
  <div class="bg-white rounded-lg shadow-md overflow-hidden">
    <div class="px-4 py-5 sm:p-6">
      <div class="flex items-center justify-between mb-4">
        <h3 class="text-lg font-medium text-gray-900">
          イベント一覧
        </h3>
        <div class="text-sm text-gray-500">
          合計 <%= @events.total_count if @events.respond_to?(:total_count) || Event.count %> 件
        </div>
      </div>
    </div>

    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              イベント名
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              使用ギフト記録数
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              作成日
            </th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              アクション
            </th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @events.each do |event| %>
            <tr class="hover:bg-gray-50">
              <!-- イベント名 -->
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="flex items-center">
                  <div class="flex-shrink-0">
                    <div class="w-10 h-10 rounded-lg bg-orange-100 flex items-center justify-center">
                      <i class="fas fa-calendar-day text-orange-600"></i>
                    </div>
                  </div>
                  <div class="ml-4">
                    <div class="text-sm font-medium text-gray-900">
                      <%= event.display_name %>
                    </div>
                    <% if event.name.length > 30 %>
                      <div class="text-sm text-gray-500">
                        ID: <%= event.id %>
                      </div>
                    <% end %>
                  </div>
                </div>
              </td>

              <!-- 使用ギフト記録数 -->
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="flex items-center">
                  <span class="text-sm font-medium text-gray-900">
                    <%= event.gift_records_count %>
                  </span>
                  <span class="text-sm text-gray-500 ml-1">件</span>
                  <% if event.gift_records_count > 0 %>
                    <span class="ml-2 inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                      使用中
                    </span>
                  <% else %>
                    <span class="ml-2 inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                      未使用
                    </span>
                  <% end %>
                </div>
              </td>

              <!-- 作成日 -->
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                <%= l(event.created_at, format: :short) %>
              </td>

              <!-- アクション -->
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                <%= link_to "編集", edit_admin_event_path(event), 
                    class: "text-blue-600 hover:text-blue-900" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <!-- ページネーション -->
    <% if @events.respond_to?(:current_page) && @events.total_pages > 1 %>
      <div class="px-6 py-4 border-t border-gray-200">
        <nav class="flex items-center justify-between">
          <!-- 前のページボタン -->
          <div class="flex-1 flex justify-start">
            <% if @events.prev_page %>
              <%= link_to admin_events_path(page: @events.prev_page),
                  class: "relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 transition-colors" do %>
                <i class="fas fa-chevron-left mr-2"></i>
                前へ
              <% end %>
            <% else %>
              <span class="relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-400 bg-gray-100 cursor-not-allowed">
                <i class="fas fa-chevron-left mr-2"></i>
                前へ
              </span>
            <% end %>
          </div>

          <!-- ページ番号 -->
          <div class="hidden md:flex">
            <% start_page = [@events.current_page - 2, 1].max %>
            <% end_page = [start_page + 4, @events.total_pages].min %>
            <% start_page = [end_page - 4, 1].max if end_page - start_page < 4 %>

            <!-- ページ番号範囲 -->
            <% (start_page..end_page).each do |page_num| %>
              <% if page_num == @events.current_page %>
                <span aria-current="page" class="relative inline-flex items-center px-3 py-2 border border-orange-500 bg-orange-50 text-sm font-medium text-orange-600">
                  <%= page_num %>
                </span>
              <% else %>
                <%= link_to admin_events_path(page: page_num),
                    class: "relative inline-flex items-center px-3 py-2 border border-gray-300 bg-white text-sm font-medium text-gray-500 hover:bg-gray-50" do %>
                  <%= page_num %>
                <% end %>
              <% end %>
            <% end %>
          </div>

          <!-- 次のページボタン -->
          <div class="flex-1 flex justify-end">
            <% if @events.next_page %>
              <%= link_to admin_events_path(page: @events.next_page),
                  class: "ml-3 relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 transition-colors" do %>
                次へ
                <i class="fas fa-chevron-right ml-2"></i>
              <% end %>
            <% else %>
              <span class="ml-3 relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-400 bg-gray-100 cursor-not-allowed">
                次へ
                <i class="fas fa-chevron-right ml-2"></i>
              </span>
            <% end %>
          </div>
        </nav>

        <!-- ページ情報 -->
        <div class="mt-3 text-center text-sm text-gray-700">
          <%= @events.current_page %>/<%= @events.total_pages %>ページ 
          （全<%= @events.total_count %>件中 
          <%= (@events.current_page - 1) * @events.limit_value + 1 %>-<%= [@events.current_page * @events.limit_value, @events.total_count].min %>件を表示）
        </div>
      </div>
    <% end %>
  </div>

  <!-- 空の状態表示 -->
  <% if @events.empty? %>
    <div class="bg-white rounded-lg shadow-md p-12 text-center">
      <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
        <i class="fas fa-calendar-alt text-2xl text-gray-400"></i>
      </div>
      <h3 class="text-lg font-medium text-gray-900 mb-2">イベントがありません</h3>
      <p class="text-gray-500 mb-4">
        最初のイベントを作成してギフト記録で使用できるようにしましょう。
      </p>
      <%= link_to new_admin_event_path,
          class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-orange-600 hover:bg-orange-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500" do %>
        <i class="fas fa-plus mr-2"></i>
        最初のイベントを作成
      <% end %>
    </div>
  <% end %>

  <!-- ヒント -->
  <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
    <div class="flex">
      <div class="flex-shrink-0">
        <i class="fas fa-info-circle text-blue-400"></i>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-blue-800">
          イベント管理について
        </h3>
        <div class="mt-2 text-sm text-blue-700">
          <ul class="list-disc pl-5 space-y-1">
            <li>イベントはギフト記録の分類に使用されます</li>
            <li>既にギフト記録で使用されているイベントは削除できません</li>
            <li>よく使われるイベント例：誕生日、記念日、クリスマス、母の日、父の日など</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>