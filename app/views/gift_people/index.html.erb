<% content_for(:title, "ギフト相手一覧") %>

<!-- メインコンテナ - 中央寄せ -->
<div class="max-w-7xl mx-auto">
<!-- ヘッダー部分 -->
<div class="mb-2 mt-2 text-center">
  <h2 class="text-2xl font-bold text-gray-800 mb-3">
    ギフト相手一覧
  </h2>
  <div class="flex justify-center">
    <%= link_to new_gift_person_path,
        class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white btn-primary hover:bg-primary-hover focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary" do %>
      <i class="fas fa-plus mr-2"></i>
      新しい相手を追加
    <% end %>
  </div>
</div>

  <!-- 検索・フィルター機能 -->
  <%= render 'shared/filter_form',
      form_url: gift_people_path,
      relationship_options: @relationship_options,
      event_options: @event_options,
      total_records: @total_people,
      sort_by_options: [["ギフト記録数", "gift_records_count"]],
      search_placeholder: "名前、好きなもの、メモで検索",
      search_value: h(params[:search]),
      search_input_id: "search-input",
      search_dropdown_id: "autocomplete-dropdown",
      search_loading_id: "search-loading",
      search_autocomplete_data: { 'gift-records-autocomplete-endpoint-value': '/gift_people/autocomplete' },
      field_id_prefix: 'people',
      filter_type_options: [["選択してください", ""], ["関係性", "relationship"], ["イベント", "event"], ["年齢", "age"], ["性別", "gender"]],
      age_options: [["10代", "10s"], ["20代", "20s"], ["30代", "30s"], ["40代", "40s"], ["50代", "50s"], ["60代", "60s"], ["70代", "70s"], ["80代以上", "80plus"]],
      gender_options: [["男性", "male"], ["女性", "female"]],
      active_filter_params: [:search, :relationship_id, :event_id, :age_group, :gender, :filter_type, :sort_by]
    %>
  
  <!-- 補助情報 -->
  <div class="text-center text-sm text-gray-600 mb-4 max-w-2xl mx-auto">
    <p>
      <i class="fas fa-info-circle mr-1 text-gray-400"></i>
      ギフト相手を登録しておくと、ギフト記録作成時に選択できるようになります
    </p>
  </div>
  
  <!-- ギフト相手一覧 -->
  <div class="text-center mb-3 mt-1">
    <h3 class="text-base font-medium text-gray-800">
      <i class="fas fa-address-book mr-1 text-primary"></i>
      登録済み相手
      <span class="text-sm text-gray-500 font-normal ml-1">
        （<%= pluralize(@total_people, "人") %>）
      </span>
    </h3>
  </div>

  <% if @gift_people.any? %>
    <!-- カードグリッドレイアウト（PCで6列表示／モバイルはカード幅を少し小さく） -->
    <div class="grid grid-cols-2 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-6 xl:grid-cols-6 gap-2 sm:gap-3 md:gap-4 justify-items-center">
      <% @gift_people.each do |person| %>
        <div class="card bg-white shadow-md compact hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 gift-person-card-fixed cursor-pointer w-[100%] sm:w-40 md:w-full" onclick="window.location.href='<%= gift_person_path(person) %>'">

          <!-- 相手情報 -->
          <div class="p-2 sm:p-3 md:p-4">
            <!-- プロフィール画像 -->
            <div class="flex justify-center mb-2 sm:mb-3 md:mb-4">
              <% if person.has_avatar? && display_avatar(person, :medium) %>
                <%= image_tag display_avatar(person, :medium), 
                    class: "w-16 h-16 sm:w-18 sm:h-18 md:w-20 md:h-20 rounded-full object-cover border-3 border-gray-200 shadow-md",
                    alt: "#{person.name}のプロフィール画像",
                    loading: "lazy" %>
              <% else %>
                <div class="w-16 h-16 sm:w-18 sm:h-18 md:w-20 md:h-20 rounded-full bg-gray-100 border-3 border-gray-200 flex items-center justify-center shadow-md">
                  <i class="fas fa-user text-lg sm:text-xl md:text-2xl text-gray-400"></i>
                </div>
              <% end %>
            </div>
            <!-- 名前 -->
            <div class="mb-2">
              <h4 class="text-sm sm:text-base md:text-lg font-bold leading-tight text-gray-800">
                <%= truncate(person.name, length: 11) %>
              </h4>
            </div>

            <!-- 関係性 -->
            <div class="mb-2">
              <div class="flex items-center text-sm">
                <i class="fas fa-users mr-2 text-indigo-500"></i>
                <span class="text-gray-600">
                  <%= person.relationship&.name || "未設定" %>
                </span>
              </div>
            </div>

            <!-- 誕生日（パソコン版のみ表示） -->
            <% if person.birthday.present? %>
              <div class="mb-2 hidden md:block">
                <div class="flex items-center text-sm">
                  <i class="fas fa-birthday-cake mr-2 text-yellow-500"></i>
                  <span class="text-gray-600">
                    <%= l(person.birthday, format: :default) %>
                    <% if person.display_age_at.present? %>
                      <span class="text-xs text-gray-500">(<%= person.display_age_at %>)</span>
                    <% end %>
                  </span>
                </div>
              </div>
            <% end %>

            <!-- 好きなもの（短縮表示） -->
            <% if person.likes.present? %>
              <div class="mb-2">
                <div class="flex items-start text-sm">
                  <i class="fas fa-thumbs-up mr-2 text-green-500 mt-0.5"></i>
                  <span class="text-gray-600 flex-1">
                    <span class="block md:hidden"><%= truncate(person.likes, length: 20) %></span>
                    <span class="hidden md:block"><%= truncate(person.likes, length: 40) %></span>
                  </span>
                </div>
              </div>
            <% end %>

            <!-- ギフト記録数 -->
            <% gift_count = current_user.gift_records.where(gift_people_id: person.id).count %>
            <div class="mt-2 sm:mt-3 pt-1.5 sm:pt-2 border-t border-gray-200">
              <div class="flex items-center justify-between text-xs sm:text-sm">
                <span class="text-gray-500">ギフト記録</span>
                <span class="font-medium text-primary">
                  <%= pluralize(gift_count, "件") %>
                </span>
              </div>
            </div>

            <!-- 最後のギフト記録日 -->
            <% last_gift = current_user.gift_records.where(gift_people_id: person.id).order(gift_at: :desc).first %>
            <% if last_gift %>
              <div class="mt-0.5 sm:mt-1">
                <div class="flex items-center justify-between text-xs text-gray-500">
                  <span>最後のギフト</span>
                  <span><%= l(last_gift.gift_at, format: :short) %></span>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
    
    <!-- ページネーション -->
  <% if @gift_people.respond_to?(:current_page) %>
    <%= render 'shared/pagination',
        collection: @gift_people,
        page_path: ->(page_number) { gift_people_path(@pagination_params.merge(page: page_number)) } %>
  <% end %>
    
  <% else %>
    <% filter_keys = [:search, :relationship_id, :event_id, :age_group, :gender, :filter_type, :sort_by] %>
    <% has_filters = filter_keys.any? { |key| params[key].present? } %>
    <% secondary_action = if has_filters
         {
           url: gift_people_path,
           icon: 'fas fa-list',
           label: 'すべて表示',
           class: 'inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-gray-600 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 mr-3'
         }
       end %>
    <% primary_action = {
         url: new_gift_person_path,
         icon: 'fas fa-plus',
         label: has_filters ? '新しい相手を登録' : '最初の相手を登録',
         class: 'inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white btn-primary hover:bg-primary-hover focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary'
       } %>

    <% empty_title = has_filters ? '検索結果が見つかりませんでした' : 'ギフト相手が登録されていません' %>
    <% empty_body = has_filters ? '検索条件を変更して再度お試しください。' : '最初のギフト相手を登録して、ギフト記録の管理を始めましょう。' %>

    <div class="gift-card-container col-span-full flex justify-center">
      <div class="w-[200px] sm:w-[240px] md:w-[260px]">
        <%= render 'shared/empty_state',
            icon: 'fas fa-users',
            title: empty_title,
            body: empty_body,
            primary_action: primary_action,
            secondary_action: secondary_action %>
      </div>
    </div>
  <% end %>
</div>

</div>
