<% content_for(:title, t('.title')) %>

  <%# <div style="
    width: 90%;
    max-width: 600px;
    padding: 40px 30px;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    text-align: center;
    margin: 0 auto;
    position: relative;
  "> %>
    <div style="
    width: 90%;
    max-width: 600px;
    padding: 40px 30px;
    border-radius: 12px;
    text-align: center;
    margin: 0 auto;
    position: relative;
  ">
    <h1 style="
      margin: 0 0 40px 0;
      padding: 0;
      font-size: 28px;
      font-weight: 600;
      color: #333;
      line-height: 1.4;
    "><%= t('.title') %></h1>

    <%= render 'shared/error_messages', object: @gift_record %>
    <%= render 'form', gift_record: @gift_record %>
  </div>
</div>
